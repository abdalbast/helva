const Navigation = () => {
  return (
    <nav className="col-span-12 flex justify-between items-start border-b border-foreground/10 pb-5 mb-10">
      <div className="font-display font-extrabold text-2xl tracking-tighter uppercase">
        Helva
      </div>
      <div className="font-mono text-[0.7rem] uppercase tracking-[0.2em] text-muted-foreground">
        Stockholm // Digital Architecture // 2024
      </div>
    </nav>
  );
};

export default Navigation;
